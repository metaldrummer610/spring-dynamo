package com.group1001.daap.dynamo

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test
import org.springframework.boot.autoconfigure.AutoConfigurations
import org.springframework.boot.test.context.runner.ApplicationContextRunner

class AutoconfigurationTest {
//    @Test
//    fun `normal config should give us expected classes`() {
//        val runner = ApplicationContextRunner().withConfiguration(AutoConfigurations.of(DynamoDBConfig::class.java))
//        runner.run {
//            assertThat(it).hasBean("testEntityRepository").isInstanceOf(DefaultCompositeKeyRepository::class.java)
//        }
//    }
//
//    @Test
//    fun `test config should give us expected classes`() {
//        val runner = ApplicationContextRunner().withConfiguration(AutoConfigurations.of(DynamoTestConfiguration::class.java))
//        runner.run {
//            assertThat(it).hasBean("testEntityRepository").isInstanceOf(CompositeInMemoryRepository::class.java)
//        }
//    }
}
